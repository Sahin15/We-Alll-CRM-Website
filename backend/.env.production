# Production Environment Variables for Hostinger VPS
# Copy this file to your Hostinger VPS and rename to .env
# Update all values with your actual credentials

# MongoDB Atlas Connection
# Get this from MongoDB Atlas: Cluster → Connect → Connect your application
# Note: @ symbol in password is URL-encoded as %40
MONGO_URI=mongodb+srv://sahinmondal7_db_user:Sahin%40mongodb123@wealll-crm.dkhsqtu.mongodb.net/crm-database?retryWrites=true&w=majority&appName=wealll-crm

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration
# IMPORTANT: Generate a new strong random string for production!
# Use: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_STRING_IN_PRODUCTION
JWT_EXPIRE=30d

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=AKIATSRYUTOO7ERQHO6V
AWS_SECRET_ACCESS_KEY=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
AWS_REGION=eu-north-1
AWS_S3_BUCKET_NAME=wealll-crm-aws

# Email Configuration (Optional - for future use)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_email_app_password

# SECURITY NOTES:
# 1. Never commit this file to git
# 2. Keep credentials secure
# 3. Change JWT_SECRET to a strong random string
# 4. Whitelist Hostinger VPS IP in MongoDB Atlas Network Access
