# Legacy letterhead assets

Offer letter PDFs no longer use PDFKit or `letterhead-background.png`.

**Active assets:** `backend/src/assets/images/` (see that folder’s README).

**This folder** may still hold reference files (e.g. `Wealll-Seal.png`). The PDF pipeline prefers `src/assets/images/seal.png`.
