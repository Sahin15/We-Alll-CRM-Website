/**
 * Authorization V2 — Startup configuration validation (Phase 7).
 * Warn-only; does not block server boot.
 */

import { runStartupAuthzValidation } from './configValidator.js';

export { runStartupAuthzValidation };
