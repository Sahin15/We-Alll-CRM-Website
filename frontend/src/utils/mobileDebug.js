/**
 * Mobile Debug Helper
 * Shows errors on screen for mobile debugging
 */

let errorContainer = null;

export const initMobileDebug = () => {
  // Disabled in production - use browser dev tools instead
  // Mobile debugging should be done in development mode
  return;
};

const showError = (message, isError = true) => {
  // Disabled - no longer showing errors on screen
  return;
};

export const logDebug = (message) => {
  // Disabled - no longer showing debug messages on screen
  return;
};
